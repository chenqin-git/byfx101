<div class="col-md-12">
  <h2>项目管理</h2>
  <div class="group">
    <%= link_to("新建项目", new_admin_project_path, class: "btn btn-primary pull-right") %>
  </div>
  <table class="table table-hover">
    <thead>
      <tr>
        <th>#</th>
        <th>名称</th>
        <th>描述</th>
      </tr>
    </thead>
    <tbody>
      <% @projects.each do |project| %>
        <tr>
          <td>#</td>
          <td><%= link_to(project.name, admin_project_path(project)) %></td>
          <td><%= project.remark %></td>
          <td>
              <%= link_to("编辑", edit_admin_project_path(project), class: "btn btn-sm btn-default")%>
              <%= link_to("删除", admin_project_path(project),    class: "btn btn-sm btn-default",
                          method: :delete, data: { confirm: "Are you sure?" } )%>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
