<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <a class="navbar-brand" href="/">三毛分销交易平台</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <% if !current_user %>
                      <li><%= link_to("注册", new_user_registration_path) %> </li>
                      <li><%= link_to("登入", new_user_session_path) %></li>
                    <% else %>
                      <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            Hi!, <%= current_user.email %> | ¥<%= current_user.balance! %>
                            <b class="caret"></b>
                        </a>
                        <ul class="dropdown-menu">
                          <li><%= link_to("我的项目", projects_path) %></li>
                          <li><%= link_to("我的订单", account_orders_path) %></li>
                          <li><%= link_to("消费记录", account_account_books_path) %></li>

                          <% if current_user && current_user.is_admin? %>
                            <li class="divider"> </li>
                            <li><%= link_to("用户管理", admin_users_path) %></li>
                            <li><%= link_to("项目管理", admin_projects_path) %></li>
                            <li><%= link_to("订单管理", admin_orders_path) %></li>
                            <li><%= link_to("分销商等级管理", admin_agent_ranks_path) %></li>
                            <li><%= link_to("价格表管理", admin_quotations_path) %></li>
                          <% end %>

                          <li class="divider"> </li>
                          <li> <%= link_to("登出", destroy_user_session_path, method: :delete) %> </li>
                        </ul>

                      </li>
                    <% end %>
                </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
